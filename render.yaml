services:
  - type: web
    name: fileflow-server
    env: node
    plan: free
    buildCommand: npm install --prefix server && npm run build:client
    startCommand: npm start --prefix server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
